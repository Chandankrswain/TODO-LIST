import { Input } from "@chakra-ui/react";

const InputBar = () => {
  return (
    <Input
      margin="10px"
      variant="filled"
      size="lg"
      width="auto"
      placeholder="Enter the Task"
    />
  );
};

export default InputBar;
